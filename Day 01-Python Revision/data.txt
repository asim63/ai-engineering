Opening a file, creates a new one by itself
Opening a file, creates a new one by itself
Opening a file, creates a new one by itself
Opening a file, creates a new one by itself
Opening a file, creates a new one by itself
Opening a file, creates a new one by itself
Opening a file, creates a new one by itself
Opening a file, creates a new one by itself
